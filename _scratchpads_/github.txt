Global setup:

 Set up git # http://help.github.com/set-up-git-redirect
  git config --global user.name "Daniel Huffman"
  git config --global user.email drhuffman12@yahoo.com
      

Next steps:

  mkdir drh-test-ssl
  cd drh-test-ssl
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:drhuffman12/drh-test-ssl.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:drhuffman12/drh-test-ssl.git
  git push -u origin master
      

Importing a Subversion Repo?

  Click here # https://github.com/drhuffman12/drh-test-ssl/imports/new
      

When you're done:

  Continue # https://github.com/drhuffman12/drh-test-ssl

 
